import React, {useEffect, useState}  from 'react';

export default () => {
    const [loggedOut, setLoggedOut] = useState(false);
    useEffect(() => {
        // TODO Logout
    });
    return (
        <>
            <h1> Logging out... </h1>
        </>
    )
    
};